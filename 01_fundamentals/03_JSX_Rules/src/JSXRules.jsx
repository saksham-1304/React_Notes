import React from 'react'

const JSXRules = () => {
    return (
        <div>
            <h1>JSXRules</h1>
            <p>Some important JSX rules:</p>
            <ul>
                <li>JSX must return a single parent element</li>
                <li>JSX elements must be properly closed</li>
                <li>JSX attributes must be written in camelCase (e.g. className instead of class)</li>
            </ul>
        </div>
    )
}

export default JSXRules;